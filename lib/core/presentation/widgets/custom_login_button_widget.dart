import 'package:flutter/material.dart';

class CustomLoginButtonWidget extends StatelessWidget {
  const CustomLoginButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}